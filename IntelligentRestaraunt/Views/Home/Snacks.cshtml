@model IEnumerable<IntelligentRestaraunt.Models.productViewModel>
@{
    ViewBag.Title = "Snacks";
}

<div class="jumbotron">
    <h2>The list of snacky snacks on the side</h2>

</div>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.productName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.productDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.productPrice)

        </th>

    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.productName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.productDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.productPrice)
            </td>
            <td>
                <a id="btn_add" href="@Url.Action("Add", "AddToCart", item)" class="btn btn-info btn-lg">
                    <span class="addToOrder"></span> Add To Order
                </a>

            </td>
        </tr>
    }

</table>